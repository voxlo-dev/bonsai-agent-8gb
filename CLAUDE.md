# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Claude Code reads this file, not `AGENTS.md`. The imports below are the shim: they pull in the
agent-agnostic project guide and the local handoff note, so both stay single-sourced.

@AGENTS.md

@CHECKPOINT.local.md
